// Modèle Guide_Category (table de liaison)

const sequelize = require('../database');
const Guide_Category = sequelize.define('Guide_Category', {
  
}, {
  // Options du modèle
});

module.exports = Guide_Category;